services:
  - type: web
    name: ayurvitalai-backend
    env: docker
    dockerfilePath: ./healthtech-backend/Dockerfile
    dockerContext: ./healthtech-backend
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: FRONTEND_URL
        sync: false
      - key: SECRET_KEY
        generateValue: true
