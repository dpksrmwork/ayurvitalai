services:
  - type: web
    name: ayurvitalai-backend
    env: docker
    rootDir: healthtech-backend
    dockerfilePath: Dockerfile
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: FRONTEND_URL
        sync: false
      - key: SECRET_KEY
        generateValue: true
